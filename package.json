{
  "name": "ar-hide-and-seek",
  "version": "1.0.0",
  "description": "AR Hide and Seek - Web AR hide & seek PoC (MindAR + three.js, async via node:sqlite)",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node tools/check-mask.js && node tools/check-palette.mjs && node tools/check-seek.mjs && node tools/check-linearmap-demo.mjs && node tools/check-pose.mjs"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "three": "^0.160.0"
  }
}
